import 'package:flutter/material.dart';

class BodyViewcontact extends StatelessWidget {
  const BodyViewcontact({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}